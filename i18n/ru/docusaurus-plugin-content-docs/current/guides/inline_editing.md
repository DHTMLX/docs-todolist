---
sidebar_label: Встроенное редактирование
title: Встроенное редактирование
description: You can learn about inline editing in the documentation of the DHTMLX JavaScript To Do List library. Browse developer guides and API reference, try out code examples and live demos, and download a free 30-day evaluation version of DHTMLX To Do List.
---

# Встроенное редактирование {#inline-editing}

Дважды щёлкните по задаче или проекту, чтобы отредактировать их в режиме встроенного редактирования. Встроенное редактирование включено по умолчанию.

:::tip
При включении режима [`readonly`](api/configs/readonly_config.md) в To Do List встроенное редактирование становится недоступным.
:::

## Поддерживаемые форматы данных {#supported-formats-of-data}

Встроенный редактор принимает три типа данных: текст и числа, хэштеги и даты.

### Текст и числа {#text-and-numbers}

Вводите любой текст или числа непосредственно во встроенный редактор.

### Хэштеги {#hashtags}

Используйте хэштеги для категоризации задач и улучшения их поиска. Соблюдайте следующие правила:

- хэштег объединяет символ `#` с буквами и цифрами (например, `#sport`)
- чтобы создать хэштег, введите `#`, затем хотя бы один символ (например, `#t`) и нажмите Enter
- To Do List поддерживает неограниченное количество хэштегов, разделённых пробелами (например, `#tag #tag1`)
- все хэштеги доступны во всех проектах
- чтобы просмотреть список хэштегов, введите символ `#`
- чтобы добавить хэштег к задаче, нажмите на него в списке
- чтобы активировать строгую фильтрацию, нажмите на хэштег или введите его в строку поиска на панели инструментов

:::info
Создайте список тегов по умолчанию с помощью свойства [`tags`](api/configs/tags_config.md).
:::

### Даты {#dates}

Чтобы ввести дату через встроенный редактор, введите символ `!`. На странице появится встроенный выбор даты.

Также можно обернуть текст в специальные символы `!()`. Например, введите `!(Enter Booking date)` и нажмите Enter, чтобы закрыть редактор. Нажмите на этот текст позже, чтобы снова открыть выбор даты.

:::info
Отображение даты зависит от [`locale`](api/configs/locale_config.md) и [формата даты](api/configs/taskshape_config.md).
:::

## Работа с редактором {#working-with-editor}

Чтобы открыть редактор для конкретной задачи или проекта, используйте метод [`openInlineEditor()`](api/methods/openinlineeditor_method.md). Фрагмент ниже открывает редактор для указанной задачи:

~~~js
list.openInlineEditor({ id: "1.1.1" });
~~~

Чтобы закрыть редактор и сохранить изменения, передайте id элемента в метод [`closeInlineEditor()`](api/methods/closeinlineeditor_method.md):

~~~js
list.closeInlineEditor({
    id: "1.1.1"
});
~~~

Чтобы закрыть редактор без сохранения изменений, установите параметр `save` в `false`. Пример ниже отменяет изменения при закрытии:

~~~js
list.closeInlineEditor({
    id: "1.1.1",
    save: false
});
~~~

## Отслеживание изменений в редакторе {#track-editor-changes}

Подпишитесь на событие [`edit-item`](api/events/edititem_event.md), чтобы отслеживать значение, вводимое во встроенный редактор. Событие срабатывает по мере ввода пользователя, до сохранения значения.

Фрагмент ниже записывает в консоль текущее значение редактора для задачи или проекта:

~~~js
list.api.on("edit-item", ({ id, currentValue }) => {
    console.log("Current value of", id, "is", currentValue);
});
~~~

## Сочетания клавиш {#keyboard-shortcuts}

:::info
Подробнее см. в руководстве [**Навигация с клавиатуры**](guides/keyboard_navigation.md).
:::
